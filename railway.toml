[build]
builder = "nixpacks"
buildCommand = "npx prisma generate && npm run build && npm run build:server"

[deploy]
startCommand = "npx prisma migrate deploy && node dist/server/index.js"
restartPolicyType = "on_failure"

[[deploy.environmentVariables]]
name = "NODE_ENV"
value = "production"
