[build]
builder = "nixpacks"
buildCommand = "npx prisma generate"

[deploy]
startCommand = "npx prisma migrate deploy && npx tsx server/index.ts"
restartPolicyType = "on_failure"

[[deploy.environmentVariables]]
name = "NODE_ENV"
value = "production"
